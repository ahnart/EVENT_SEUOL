$(document).ready(function () {

    /*
    $(".st_popup .reg_popup > button").click(function () {
        $(".st_popup").stop().hide();
    });

    // 참여버튼 클릭시 텍스트애니 시작
    $(".st_popup .reg_popup > button").click(function(){
        $("#wrap .logo img, .event_date, .headcopy, .com_logo").toggleClass("on")
    });
    
    // hover시 GIF시작
     $(".hov-anim, .hov-anim_end").click(function() {
            $(this).attr("src", $(this).data("animated"))
        });
    
    
    // 마지막 gif 실행시 담청결과 팝업창 출력
    $(".hov-anim_end").click(function(){
        $(".result_popup").stop().fadeIn();
    });
    
    $(".result_popup .end_popup > button").click(function () {
        $(".result_popup").stop().hide();
    });
*/
}); //ready end
